{
  "name": "accessiblemaps",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && nodemon app.js"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
}
